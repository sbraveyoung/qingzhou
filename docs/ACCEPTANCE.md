# 轻舟 — 待验收清单 / Acceptance Checklist

> 2026-07-03 决策：组织版 Apple Developer 账号已注册完成，原「#11B 等 App Store 正式版再验收」
> 策略**取消**——所有待验收项**现在就验收**，用 TestFlight / 开发版真机即可，不再等正式版。
>
> 用法：逐项执行「操作步骤」，对照「预期结果」，通过就把「验收状态」勾上并注明日期/设备。
> 一项不过就在该项下面记现象（截图/日志路径），别整批标失败。
> 相关背景见 [ROADMAP.md](ROADMAP.md) 当前状态小节。

**通用前置条件**（下面每项默认都需要，不再重复写）：

- iPhone 真机（VPN 扩展模拟器跑不了）+ Xcode 开发版安装或 TestFlight 构建
- 至少一个真实可用节点 / 订阅源
- 装完新构建后**关开一次 VPN**，确保旧扩展进程已被替换

## 功能速览（每项是什么 / 解决什么问题）

| # | 功能 | 一句话：是什么 / 解决什么 |
|---|---|---|
| 1.1 | 经代理延迟 | 原测速只测「手机→节点」的直连 TCP（可达≠可用）；这项让扩展里的 xray **真实走该节点**请求 `google.com/generate_204`，测出用户体感的端到端延迟——直连快但出口绕路 / 密码错的「假好节点」现原形 |
| 1.2 | 配置预检 | 热切换前先校验新节点配置，坏配置**不拆在跑的隧道**——解决「切到坏节点直接全网断」的事故 |
| 1.3 | 代理/直连拆分 | 规则模式下首页流量卡分开显示走代理 / 走直连的字节和占比——回答「我的规则到底分流了多少」 |
| 1.4 | 节点导出 | 节点反向导出为分享链接——解决「配置进得来出不去」，可迁去 v2rayN 等其他客户端 |
| 2 | 流量波形 | 首页实时上下行速率波形，数据来自扩展 TUN 层逐字节计数（曾有并发 bug 算错速率，故留真机确认） |
| 3 | S2 关闭 | 路线图最老的一项「真机经隧道访问 google.com」，日常使用早已覆盖，口头确认即关闭 |
| 4 | iCloud 弹窗降噪 | 解决「每次打开 App 都弹恢复确认」：内容一致静默采认 + 拒绝过的版本不再弹；仅云端**真有**用户内容变化才弹 |
| 5 | 隧道状态采认 | 解决「杀掉重开 App 后开关假显示关闭（流量其实还在代理）」：启动时与系统 VPN 真实状态对齐 ✅ 已远程实证 |
| 6.1-6.4 | 小组件全家桶 | 不打开 App 也能看状态 / 一键启停：主屏三尺寸、锁屏、iOS 18 控制中心、macOS 通知中心 |
| 6.5 | 快捷指令 + Siri | 开/关/切换三个动作零配置出现在快捷指令 App，Siri 说「开启轻舟」即连 |
| 6.6 | 状态查询动作 | 给自动化做条件分支：「如果未连接才开启」，避免重复启动 |
| 6.7 | macOS 打开 App 自动连 | macOS 快捷指令没有「App 打开/关闭」触发器，我们原生实现：打开指定 App 自动连、全退自动断（只断自动拉起的会话） |

---

## 1. #11B libXray 能力接入 · 四项（已合 main @ 1106005）

> 背景：合并后 526 单测全绿 + iOS/macOS 双平台编译通过，但四项功能都没在真机上人工验收过。

### 1.1 经代理延迟

- **前置条件**：VPN 已开启且节点可用；设置页诊断区可见扩展内存 footprint
- **操作步骤**：
  1. 节点页长按某节点 → 「测经代理延迟」
  2. 工具栏「⋯」→ 批量测经代理延迟，观察进度
  3. 测速过程中去 设置 → 诊断区 看扩展内存 footprint
  4. 关闭 VPN，回到节点页确认入口状态；再跑一次原直连测速
- **预期结果**：
  - 长按单测后该节点出现**第二个延迟 chip**（经代理延迟，与直连延迟并列）
  - 批量测速有进度反馈，全部节点跑完
  - **扩展内存 footprint 不逼近 40MB**（50MB 硬上限，超了系统杀进程=断流）
  - 关 VPN 时「经代理延迟」入口禁用；原直连测速行为不变
- **验收状态**：
  - [ ] 通过（日期/设备：＿＿＿）

### 1.2 配置预检

- **前置条件**：准备一个**坏配置节点**（如故意改错端口/密码的节点）+ 一个好节点
- **操作步骤**：
  1. 连着好节点（VPN 开启）时，切换到坏配置节点
  2. 观察提示与网络连通性（切换时开着一个持续 ping / 视频流更直观）
  3. 关 VPN → 直接选坏节点冷启动 VPN
- **预期结果**：
  - 热切换到坏节点：提示「已保持当前节点连接不变…」且**不断网**（当前连接保持）
  - 冷启动坏节点：alert 显示 **xray 可读错误信息**（`fetchLastDisconnectError` 链路），
    不是干巴巴的「连接失败」
- **验收状态**：
  - [ ] 通过（日期/设备：＿＿＿）

### 1.3 代理 / 直连流量拆分（QueryStats）

- **前置条件**：规则代理模式；VPN 开启并产生一些混合流量（访问国内站 + 国外站）
- **操作步骤**：
  1. 规则模式下开 VPN，浏览若干国内 + 国外网站
  2. 回首页看流量卡
  3. 关 VPN，等几秒再看流量卡
  4. 顺带观察：VPN 每次启动是否正常（metrics inbound 拿不到端口时应静默不开统计，
     不能影响 VPN 启动）
- **预期结果**：
  - 首页流量卡出现「代理 / 直连」行 + 各自占比，数字随访问增长
  - 关 VPN 几秒后「代理 / 直连」行消失
  - VPN 启动稳定，无因统计端口问题导致的启动失败
- **验收状态**：
  - [ ] 通过（日期/设备：＿＿＿）

### 1.4 节点导出（反向导出分享链接）

- **前置条件**：节点列表里有多种协议的节点（trojan / vmess / vless / ss / hy2 尽量都有）
- **操作步骤**：
  1. 单节点 → 「复制分享链接」→ 粘贴回「添加节点」
  2. 「导出全部节点链接」→ 全部粘贴回「添加节点」
  3. （加分项）把导出的链接粘到其他客户端（v2rayN 等）导入一次；
     再从其他客户端导出链接粘回轻舟
- **预期结果**：
  - 粘回后**全量解析成功**，节点参数（协议/主机/端口/加密/传输层参数）与原节点一致
  - 与其他客户端互导无字段丢失（加分项，不阻塞验收）
- **验收状态**：
  - [ ] 通过（日期/设备：＿＿＿）

---

## 2. 流量波形（a1）真机验证

- **前置条件**：VPN 开启，节点可用
- **操作步骤**：
  1. 开 VPN，首页看流量波形卡
  2. 跑一个大流量场景（在线视频 / 大文件下载）
  3. 停止流量，观察波形回落
- **预期结果**：波形随真实流量起伏（不是平线/假数据），字节数持续增长，停止后波形回落到接近零
- **验收状态**：
  - [ ] 通过（日期/设备：＿＿＿）

---

## 3. S2 真机翻墙测试 · 正式勾掉

> 这是 ROADMAP 里挂了很久的「S2 唯一未完成项」（iPhone 真机访问 google.com 成功，
> 见 [S2-TESTING.md](S2-TESTING.md)）。实际上日常真机使用早已覆盖该场景，
> 本项只需用户**确认一句**即可正式关闭。

- **操作步骤**：确认「日常已在 iPhone 真机上用轻舟开 VPN 正常访问 google.com 等境外站点」属实
- **预期结果**：属实即通过
- **验收状态**：
  - [ ] 确认通过，S2 正式完成（日期：＿＿＿）

---

## 4. iCloud 恢复弹窗降噪（commit 54aae48）

> 修复内容：云端与本地内容哈希一致时**静默采认**不弹窗；点「暂不恢复」按云端 revision
> 持久化记忆；只有云端**真有用户内容变化**才再次弹恢复确认。
>
> ⚠️ **常见误解（2026-07-03 验收时踩过）**：在**本机**删订阅 → 杀掉重开 → 不弹恢复确认，
> 这是**预期行为**，不是 bug。本地编辑（包括删除）是权威操作，会以更高 revision 镜像上云，
> 云端不比本机新，自然不弹。**误删找回**走：设置 → iCloud 同步 → 「立即恢复 iCloud 数据」→
> 版本列表选删除前的历史版本（每份带「N 订阅 · M 节点」计数，一眼识别）。
> 启动弹窗只服务一个场景：**云端比本机新**（另一台设备改过配置 / 卸载重装 / 新设备）。

- **前置条件**：iPhone + Mac 同一 iCloud 账号，两端都装有包含该修复的构建，iCloud 保险柜已启用
- **操作步骤**：
  1. 制造一次恢复确认弹窗（如 Mac 改配置后 iPhone 冷启动），iPhone 弹出恢复确认时点**「暂不恢复」**
  2. 杀掉 iPhone 主 App，重新打开
  3. 在 Mac 上**改订阅**（真实内容变化），等同步后杀掉 iPhone App 重开
  4. 在 iPhone 上执行一次恢复；然后在 Mac 上**只改主题再改回去**（内容净变化为零），
     杀掉 iPhone App 重开
- **预期结果**：
  - 步骤 2：**不应再弹**恢复确认（拒绝已按 revision 持久化）
  - 步骤 3：**应弹**恢复确认（云端有真实用户内容变化）
  - 步骤 4：**不应弹**（内容哈希一致，静默采认）
- **验收状态**：
  - [ ] 通过（日期/设备：＿＿＿）

---

## 5. 启动时采认在跑的隧道（VPN 开关状态，commit e1f9df6）

> 修复内容：主 App 被杀重开 / Xcode 替换安装后，App 内开关与系统 `NEVPNStatus` 对齐，
> 不再假显示「关闭」。

- **前置条件**：装有包含该修复的构建
- **操作步骤**：
  1. 开 VPN，确认连接正常，记一下连接时长
  2. 杀掉主 App（上滑移除），重新打开
  3. VPN 保持开着，用 Xcode 替换安装一个新构建（⌘R），打开 App
- **预期结果**：
  - 步骤 2：开关显示**开**，连接**时长正确**（延续原连接，不是从 0 开始）
  - 步骤 3：同样——开关显示开、时长正确
- **验收状态**：
  - [x] 通过（2026-07-03 · iPhone 14 Pro · Claude 远程实测：DEBUG 钩子开 VPN →
    显示已连接；`--terminate-existing` 杀掉重开 → 正确采认「已连接 0:01:38」+ 开关绿；
    钩子停 VPN → 「VPN 未连接」。三态截图一致。注：替换安装（步骤 3）会被 iOS
    杀掉扩展进程 → VPN 真断，重开显示关闭属**系统行为**，非本 bug）

---

## 6. Widget / Shortcuts / macOS 自动连（已实现，待真机验收）

> 实现已全部合入本分支（Widget 全家桶 + 集成点、自动化补全 ac1bfa6）。
> **一次性前置**：两个新 bundle id（`com.sbraveyoung.qingzhou.ios.widget` /
> `com.sbraveyoung.qingzhou.mac.widget`）需要 App ID + App Group + Network Extensions
> capability。Xcode 自动签名一般会代办；⌘R 首次失败时去 developer.apple.com 手动补。
>
> **2026-07-03 首轮验收打回 4 项，已修复**（详见该日 commit）：
> 1. 快捷指令搜不到「轻舟」→ AppShortcutsProvider 从 SPM 包移到 app target
>    （包内不被元数据抽取，`autoShortcuts` 为空的实锤已复验修复）；
> 2. widget / 控制中心点开关后卡「切换中」→ intent 现在**等 NEVPNStatus 稳态再返回**，
>    WidgetKit 重载落在最终状态；
> 3. widget 只有一种尺寸 → 补 systemMedium / systemLarge；
> 4. intent 全链路加 os_log：Console.app 过滤 subsystem
>    `com.sbraveyoung.qingzhou.intents` 可直接看失败原因（控制中心若再空转按此诊断）。

### 6.1 主屏小组件（iOS systemSmall）

- **操作步骤**：长按主屏 → 小组件库搜「轻舟」→ 添加 systemSmall；对照主 App 核对状态 /
  节点名 / 已连接时长；点「连接/断开」按钮；点非按钮区域；再回主 App 里开关一次 VPN
- **预期结果**：状态与主 App 一致；按钮真实启停 VPN，几秒内 widget 自刷（过渡态 15s 补刷）；
  点非按钮区域打开主 App；主 App 内开关 VPN 后 widget **立即**刷新（不等 30 分钟兜底）
- **验收状态**：
  - [ ] 通过（日期/设备：＿＿＿）

### 6.2 iOS 锁屏小组件（accessoryCircular / Rectangular）

- **操作步骤**：锁屏长按 → 自定义 → 添加轻舟圆形 + 矩形小组件；点按
- **预期结果**：显示连接状态；点按解锁后进主 App
- **验收状态**：
  - [ ] 通过（日期/设备：＿＿＿）

### 6.3 iOS 18 控制中心 ControlWidget

- **前置条件**：iOS 18+ 设备
- **操作步骤**：控制中心 → 添加控件 → 「轻舟 VPN」；拨动开关两个方向
- **预期结果**：开关能启停 VPN、状态正确；iOS 17 设备不应出现该控件
- **验收状态**：
  - [ ] 通过（日期/设备：＿＿＿）

### 6.4 macOS 通知中心小组件

- **操作步骤**：通知中心（菜单栏时钟点开）→ 编辑小组件 → 添加轻舟 systemSmall；点按钮启停
- **预期结果**：状态正确；按钮真实启停 VPN
- **验收状态**：
  - [ ] 通过（日期/设备：＿＿＿）

### 6.5 AppShortcutsProvider（快捷指令拿来即用 + Siri）

- **操作步骤**：打开「快捷指令」App → 搜「轻舟」；对 Siri 说「开启轻舟」「关闭轻舟」
- **预期结果**：开启/关闭/切换三个动作零配置可见可用；Siri 短语直接执行
- **验收状态**：
  - [ ] 通过（日期/设备：＿＿＿）

### 6.6 GetVPNStatusIntent（自动化条件分支）

- **操作步骤**：快捷指令新建：「轻舟是否已连接」→「如果」结果为否 →「开启轻舟」；
  分别在 VPN 开 / 关两种状态下跑
- **预期结果**：返回布尔值可作条件；关闭态跑完 VPN 被拉起，开启态跑完无额外动作
- **验收状态**：
  - [ ] 通过（日期/设备：＿＿＿）

### 6.7 macOS 打开指定 App 自动连 / 全退自动断

- **操作步骤**：
  1. 设置 → macOS 集成 → 开「打开指定 App 时自动连接」→ 添加一个测试 App（如 Safari）
  2. VPN 关闭状态下打开该 App → 观察；完全退出该 App（⌘Q）→ 观察
  3. **手动**开 VPN，再打开又退出该 App → 观察
- **预期结果**：
  - 打开触发 App → 自动连接 + toast「已随触发 App 自动连接」
  - 全部退出 → 自动断开 + toast
  - 手动开的 VPN **不因触发 App 退出被误断**（只断自动拉起的会话）
- **验收状态**：
  - [ ] 通过（日期/设备：＿＿＿）

---

## 验收进度总览

| 区块 | 项数 | 已通过 |
|---|---|---|
| 1. #11B libXray 四项 | 4 | 0 |
| 2. 流量波形 a1 | 1 | 0 |
| 3. S2 正式关闭 | 1 | 0 |
| 4. iCloud 弹窗降噪 | 1 | 0 |
| 5. 隧道状态采认 | 1 | 0 |
| 6. Widget/Shortcuts/自动连 | 7 | 0 |

> 全部通过后：更新 [ROADMAP.md](ROADMAP.md) 当前状态，进入 [APP_STORE.md](APP_STORE.md) §10 上架流程。
